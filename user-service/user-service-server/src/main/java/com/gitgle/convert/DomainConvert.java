package com.gitgle.convert;

import com.gitgle.entity.UserDomain;
import com.gitgle.response.UserDomainBase;

public class DomainConvert {

    public static UserDomain convert(UserDomainBase userDomainBase){
        UserDomain userDomain = new UserDomain();
        return userDomain;
    }
}
